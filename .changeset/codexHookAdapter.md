---
"liushi-harness": minor
---

新增 Codex `apply_patch` PreToolUse/PostToolUse Hook Adapter、Workspace Binding、原生 stdin/stdout CLI Wrapper 和确定性的 `hooks.json` 配置投影。绑定必须使用已通过 G4 的精确 PlanRisk Digest，历史业务逻辑变更继续要求 G2，R4 操作始终拒绝；真实项目配置写入和受信任由 Human 控制。
