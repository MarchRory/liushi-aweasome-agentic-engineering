---
"liushi-harness": patch
---

新增包内企业 HTTPS Release Approval Authority Adapter：固定 HTTPS endpoint、Authority 标识、Bearer 凭据与超时，使用严格输入/回执校验、流式大小上限和 fail-closed 网络错误处理；不接入普通 CLI、HarnessApplication 或 npm 根导出。
