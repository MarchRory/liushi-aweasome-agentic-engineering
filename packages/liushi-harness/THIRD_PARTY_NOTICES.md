# Third-Party Notices

## canonicalize 3.0.0

`liushi-harness` bundles code from [canonicalize](https://github.com/erdtman/canonicalize) to provide RFC 8785 JSON Canonicalization Scheme support in both ESM and CommonJS builds.

The bundled work is licensed under Apache License 2.0. A copy is included at [thirdPartyLicenses/canonicalizeLicense.txt](./thirdPartyLicenses/canonicalizeLicense.txt).

## minimatch 10.2.5

`liushi-harness` uses [minimatch](https://github.com/isaacs/minimatch) as a runtime dependency for deterministic Rule path selector matching.

The work is licensed under the Blue Oak Model License 1.0.0. A copy is included at [thirdPartyLicenses/minimatchLicense.md](./thirdPartyLicenses/minimatchLicense.md).

## jsonc-parser 3.3.1

`liushi-harness` uses [jsonc-parser](https://github.com/microsoft/node-jsonc-parser) as a runtime dependency for deterministic JSON and JSONC project configuration parsing.

The work is licensed under the MIT License. A copy is included at [thirdPartyLicenses/jsoncParserLicense.txt](./thirdPartyLicenses/jsoncParserLicense.txt).

## yaml 2.9.0

`liushi-harness` uses [yaml](https://github.com/eemeli/yaml) as a runtime dependency for deterministic YAML project configuration parsing.

The work is licensed under the ISC License. A copy is included at [thirdPartyLicenses/yamlLicense.txt](./thirdPartyLicenses/yamlLicense.txt).

## Sigstore JavaScript packages

`liushi-harness` uses `sigstore` 4.1.1, `@sigstore/bundle` 4.0.0, `@sigstore/core` 3.2.1, `@sigstore/verify` 3.1.1, and `@sigstore/protobuf-specs` 0.5.0 to create and verify Executor Compatibility DSSE attestations with explicit trust material.

These works are provided by the [Sigstore JavaScript](https://github.com/sigstore/sigstore-js) and [Sigstore Protobuf Specifications](https://github.com/sigstore/protobuf-specs) projects under the Apache License 2.0. A copy is included at [thirdPartyLicenses/sigstoreLicense.txt](./thirdPartyLicenses/sigstoreLicense.txt).
