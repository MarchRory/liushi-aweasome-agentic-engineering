/** Proposal 并发冲突后允许重新加载权威聚合的次数。 */
export const ARTIFACT_PROPOSAL_CONFLICT_RESOLUTION_ATTEMPTS = 80;

/** Proposal 并发冲突后每次重新读取权威聚合前的等待毫秒数。 */
export const ARTIFACT_PROPOSAL_CONFLICT_RESOLUTION_DELAY_MS = 25;
