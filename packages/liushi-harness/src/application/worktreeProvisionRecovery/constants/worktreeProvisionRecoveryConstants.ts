/** Worktree Provision 恢复评估的 Schema 版本。 */
export const WORKTREE_PROVISION_RECOVERY_SCHEMA_VERSION = "1.0.0" as const;

/** Human 提交 Worktree Provision 恢复命令时使用的版本化类型。 */
export const WORKTREE_PROVISION_RECONCILE_COMMAND_TYPE = "worktree.provision.reconcile.v1" as const;

/** Worktree Provision 恢复证据的稳定前缀。 */
export const WORKTREE_PROVISION_RECOVERY_EVIDENCE_PREFIX = "worktree-provision-recovery" as const;
